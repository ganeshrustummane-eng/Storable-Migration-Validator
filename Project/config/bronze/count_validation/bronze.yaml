# ============================================================
# Migration Validator — Bronze Count Validation
# Contains   : row count checks for all Bronze tables.
#
# GENERATED FILE — do not hand-edit.
# Rendered from the canonical validation plans in output/plans/.
# Re-running generation upserts each table in place; it never appends.
#
# No generation timestamp is recorded here on purpose: this file is
# shared by every table, so a timestamp would make each run produce a
# diff even when nothing changed. Per-table provenance (when, which
# model) lives in the plan JSON.
# ============================================================

tables:
  inventory:
    validations:
      count_validation:
        source_table_name: inventory
        source: mssql
        source_database: mv_test_mssql
        source_schema: dbo
        sourcequery: |-
          SELECT COUNT(*) AS source_row_count FROM dbo.inventory;
        target_table_name: INVENTORY
        target: snowflake
        target_database: MV_TEST
        target_schema: PUBLIC
        targetquery: |-
          SELECT COUNT(*) AS target_row_count FROM MV_TEST.PUBLIC.INVENTORY;
  products:
    validations:
      count_validation:
        source_table_name: products
        source: mssql
        source_database: mv_test_mssql
        source_schema: dbo
        sourcequery: |-
          SELECT COUNT(*) AS source_row_count FROM dbo.products;
        target_table_name: PRODUCTS
        target: snowflake
        target_database: MV_TEST
        target_schema: PUBLIC
        targetquery: |-
          SELECT COUNT(*) AS target_row_count FROM MV_TEST.PUBLIC.PRODUCTS;
  customers:
    validations:
      count_validation:
        source_table_name: customers
        source: postgresql
        source_database: my_test_pg
        source_schema: public
        sourcequery: |-
          SELECT COUNT(*) AS source_row_count FROM public.customers;
        target_table_name: CUSTOMERS
        target: snowflake
        target_database: MV_TEST
        target_schema: PUBLIC
        targetquery: |-
          SELECT COUNT(*) AS target_row_count FROM MV_TEST.PUBLIC.CUSTOMERS;
  orders:
    validations:
      count_validation:
        source_table_name: orders
        source: postgresql
        source_database: my_test_pg
        source_schema: public
        sourcequery: |-
          SELECT COUNT(*) AS source_row_count FROM public.orders;
        target_table_name: ORDERS
        target: snowflake
        target_database: MV_TEST
        target_schema: PUBLIC
        targetquery: |-
          SELECT COUNT(*) AS target_row_count FROM MV_TEST.PUBLIC.ORDERS;
