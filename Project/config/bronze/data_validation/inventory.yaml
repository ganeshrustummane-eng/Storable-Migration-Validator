# ============================================================
# Migration Validator — Data Validation Config
# Table      : inventory
# Generated  : 2026-09-13T14:39:02.575561
# By         : ai (model: gpt-4o)
# Columns    : 8 comparable columns
#
# Validation blocks (row count is in bronze.yaml):
#   data_validation           — normalised full-scan SELECT (all columns)
#
# Note: null_pct_validation and distinct_count_validation have been
# removed. Only data_validation and count_validation are used.
#
# Normalization rules applied automatically:
#   - Boolean    : TRUE/FALSE -> '1'/'0'
#   - Numeric    : ROUND to 2 decimal places, then text
#   - Timestamp  : 'YYYY-MM-DD HH24:MI:SS'  (microseconds stripped)
#   - Timestamp_TZ: convert to UTC -> 'YYYY-MM-DD HH24:MI:SS'
#   - Date       : 'YYYY-MM-DD'
#   - Text/Char  : TRIM leading/trailing spaces
#   - UUID       : UPPER(TRIM()) — case-insensitive comparison
#   - Integer    : CAST to text
#   - JSON/JSONB : canonical serialization (jsonb::text / TO_JSON)
#   - Bytea      : hex text encoding
#   - NULL       : COALESCE -> '<<NULL>>' sentinel (ALL columns)
# ============================================================

tables:
  inventory:
    validations:

      # ── ③ / ④ Normalised data validation (all columns) ───────────
      data_validation:
        source_table_name: inventory
        source: mssql
        source_database: mv_test_mssql
        source_schema: dbo
        pksourcecolumn: id_normalized
        source_audit_column: 
        sourcequery: |
          SELECT COALESCE(CAST(CAST(id AS VARCHAR(MAX)) AS VARCHAR(MAX)), '<<NULL>>') AS id_normalized, COALESCE(CAST(CAST(product_id AS VARCHAR(MAX)) AS VARCHAR(MAX)), '<<NULL>>') AS product_id_normalized, COALESCE(CAST(LTRIM(RTRIM(warehouse_code)) AS VARCHAR(MAX)), '<<NULL>>') AS warehouse_code_normalized, COALESCE(CAST(CAST(quantity AS VARCHAR(MAX)) AS VARCHAR(MAX)), '<<NULL>>') AS quantity_normalized, COALESCE(CAST(CAST(reorder_level AS VARCHAR(MAX)) AS VARCHAR(MAX)), '<<NULL>>') AS reorder_level_normalized, COALESCE(CAST(FORMAT(last_restocked, 'yyyy-MM-dd HH:mm:ss.ffffff') AS VARCHAR(MAX)), '<<NULL>>') AS last_restocked_normalized, COALESCE(CAST(FORMAT(created_at, 'yyyy-MM-dd HH:mm:ss.ffffff') AS VARCHAR(MAX)), '<<NULL>>') AS created_at_normalized, COALESCE(CAST(FORMAT(updated_at, 'yyyy-MM-dd HH:mm:ss.ffffff') AS VARCHAR(MAX)), '<<NULL>>') AS updated_at_normalized FROM dbo.inventory;
        target_table_name: INVENTORY
        target: snowflake
        target_database: MV_TEST
        target_schema: PUBLIC
        pktargetcolumn: id_normalized
        target_audit_column: 
        targetquery: |
          SELECT COALESCE(CAST(CAST(ID AS STRING) AS STRING), '<<NULL>>') AS "id_normalized", COALESCE(CAST(CAST(PRODUCT_ID AS STRING) AS STRING), '<<NULL>>') AS "product_id_normalized", COALESCE(CAST(TRIM(WAREHOUSE_CODE) AS STRING), '<<NULL>>') AS "warehouse_code_normalized", COALESCE(CAST(CAST(QUANTITY AS STRING) AS STRING), '<<NULL>>') AS "quantity_normalized", COALESCE(CAST(CAST(REORDER_LEVEL AS STRING) AS STRING), '<<NULL>>') AS "reorder_level_normalized", COALESCE(CAST(TO_VARCHAR(LAST_RESTOCKED, 'YYYY-MM-DD HH24:MI:SS.FF6') AS STRING), '<<NULL>>') AS "last_restocked_normalized", COALESCE(CAST(TO_VARCHAR(CREATED_AT, 'YYYY-MM-DD HH24:MI:SS.FF6') AS STRING), '<<NULL>>') AS "created_at_normalized", COALESCE(CAST(TO_VARCHAR(UPDATED_AT, 'YYYY-MM-DD HH24:MI:SS.FF6') AS STRING), '<<NULL>>') AS "updated_at_normalized" FROM MV_TEST.PUBLIC.INVENTORY;

      validation_plan:
        population_scope: {}
        relationships: []
        identity:
          type: primary_key
          source_primary_keys:
          - id
          target_primary_keys:
          - ID
          candidate_keys: []
        row_hash: null
        transformations: []
        validations: []
        requires_review: false
        review_reasons: []

      transformation_validation:
        source_table_name: inventory
        source: mssql
        source_database: mv_test_mssql
        source_schema: dbo
        sourcequery: |
          SELECT 1;
        target_table_name: INVENTORY
        target: snowflake
        target_database: MV_TEST
        target_schema: PUBLIC
        targetquery: |
          SELECT 1;
      aggregate_validation:
        source_table_name: inventory
        source: mssql
        source_database: mv_test_mssql
        source_schema: dbo
        sourcequery: |
          SELECT 1;
        target_table_name: INVENTORY
        target: snowflake
        target_database: MV_TEST
        target_schema: PUBLIC
        targetquery: |
          SELECT 1;