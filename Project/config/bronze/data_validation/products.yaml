# ============================================================
# Migration Validator — Data Validation Config
# Table      : products
# Generated  : 2026-09-13T14:39:21.483079
# By         : ai (model: gpt-4o)
# Columns    : 9 comparable columns
#
# Validation blocks (row count is in bronze.yaml):
#   data_validation           — normalised full-scan SELECT (all columns)
#
# Note: null_pct_validation and distinct_count_validation have been
# removed. Only data_validation and count_validation are used.
#
# Normalization rules applied automatically:
#   - Boolean    : TRUE/FALSE -> '1'/'0'
#   - Numeric    : ROUND to 2 decimal places, then text
#   - Timestamp  : 'YYYY-MM-DD HH24:MI:SS'  (microseconds stripped)
#   - Timestamp_TZ: convert to UTC -> 'YYYY-MM-DD HH24:MI:SS'
#   - Date       : 'YYYY-MM-DD'
#   - Text/Char  : TRIM leading/trailing spaces
#   - UUID       : UPPER(TRIM()) — case-insensitive comparison
#   - Integer    : CAST to text
#   - JSON/JSONB : canonical serialization (jsonb::text / TO_JSON)
#   - Bytea      : hex text encoding
#   - NULL       : COALESCE -> '<<NULL>>' sentinel (ALL columns)
# ============================================================

tables:
  products:
    validations:

      # ── ③ / ④ Normalised data validation (all columns) ───────────
      data_validation:
        source_table_name: products
        source: mssql
        source_database: mv_test_mssql
        source_schema: dbo
        pksourcecolumn: id_normalized
        source_audit_column: 
        sourcequery: |
          SELECT COALESCE(CAST(CAST(id AS VARCHAR(MAX)) AS VARCHAR(MAX)), '<<NULL>>') AS id_normalized, COALESCE(CAST(LTRIM(RTRIM(sku)) AS VARCHAR(MAX)), '<<NULL>>') AS sku_normalized, COALESCE(CAST(LTRIM(RTRIM(product_name)) AS VARCHAR(MAX)), '<<NULL>>') AS product_name_normalized, COALESCE(CAST(LTRIM(RTRIM(category)) AS VARCHAR(MAX)), '<<NULL>>') AS category_normalized, COALESCE(CAST(CAST(unit_price AS VARCHAR(MAX)) AS VARCHAR(MAX)), '<<NULL>>') AS unit_price_normalized, COALESCE(CAST(CASE WHEN is_available = 1 THEN '1' WHEN is_available = 0 THEN '0' ELSE NULL END AS VARCHAR(MAX)), '<<NULL>>') AS is_available_normalized, COALESCE(CAST(CAST(weight_kg AS VARCHAR(MAX)) AS VARCHAR(MAX)), '<<NULL>>') AS weight_kg_normalized, COALESCE(CAST(FORMAT(created_at, 'yyyy-MM-dd HH:mm:ss.ffffff') AS VARCHAR(MAX)), '<<NULL>>') AS created_at_normalized, COALESCE(CAST(FORMAT(updated_at, 'yyyy-MM-dd HH:mm:ss.ffffff') AS VARCHAR(MAX)), '<<NULL>>') AS updated_at_normalized FROM dbo.products;
        target_table_name: PRODUCTS
        target: snowflake
        target_database: MV_TEST
        target_schema: PUBLIC
        pktargetcolumn: id_normalized
        target_audit_column: 
        targetquery: |
          SELECT COALESCE(CAST(CAST(ID AS STRING) AS STRING), '<<NULL>>') AS "id_normalized", COALESCE(CAST(TRIM(SKU) AS STRING), '<<NULL>>') AS "sku_normalized", COALESCE(CAST(TRIM(PRODUCT_NAME) AS STRING), '<<NULL>>') AS "product_name_normalized", COALESCE(CAST(TRIM(CATEGORY) AS STRING), '<<NULL>>') AS "category_normalized", COALESCE(CAST(CAST(UNIT_PRICE AS STRING) AS STRING), '<<NULL>>') AS "unit_price_normalized", COALESCE(CAST(CASE WHEN IS_AVAILABLE = TRUE THEN '1' WHEN IS_AVAILABLE = FALSE THEN '0' ELSE NULL END AS STRING), '<<NULL>>') AS "is_available_normalized", COALESCE(CAST(CAST(WEIGHT_KG AS STRING) AS STRING), '<<NULL>>') AS "weight_kg_normalized", COALESCE(CAST(TO_VARCHAR(CREATED_AT, 'YYYY-MM-DD HH24:MI:SS.FF6') AS STRING), '<<NULL>>') AS "created_at_normalized", COALESCE(CAST(TO_VARCHAR(UPDATED_AT, 'YYYY-MM-DD HH24:MI:SS.FF6') AS STRING), '<<NULL>>') AS "updated_at_normalized" FROM MV_TEST.PUBLIC.PRODUCTS;

      validation_plan:
        population_scope: {}
        relationships: []
        identity:
          type: primary_key
          source_primary_keys:
          - id
          target_primary_keys:
          - ID
          candidate_keys: []
        row_hash: null
        transformations: []
        validations: []
        requires_review: false
        review_reasons: []

      transformation_validation:
        source_table_name: products
        source: mssql
        source_database: mv_test_mssql
        source_schema: dbo
        sourcequery: |
          SELECT 1;
        target_table_name: PRODUCTS
        target: snowflake
        target_database: MV_TEST
        target_schema: PUBLIC
        targetquery: |
          SELECT 1;
      aggregate_validation:
        source_table_name: products
        source: mssql
        source_database: mv_test_mssql
        source_schema: dbo
        sourcequery: |
          SELECT 1;
        target_table_name: PRODUCTS
        target: snowflake
        target_database: MV_TEST
        target_schema: PUBLIC
        targetquery: |
          SELECT 1;