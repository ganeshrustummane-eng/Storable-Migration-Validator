# ============================================================
# Migration Validator — Data Validation Config
# Table      : customers
# Generated  : 2026-09-13T14:28:29.366081
# By         : ai (model: gpt-4o)
# Columns    : 10 comparable columns
#
# Validation blocks (row count is in bronze.yaml):
#   data_validation           — normalised full-scan SELECT (all columns)
#
# Note: null_pct_validation and distinct_count_validation have been
# removed. Only data_validation and count_validation are used.
#
# Normalization rules applied automatically:
#   - Boolean    : TRUE/FALSE -> '1'/'0'
#   - Numeric    : ROUND to 2 decimal places, then text
#   - Timestamp  : 'YYYY-MM-DD HH24:MI:SS'  (microseconds stripped)
#   - Timestamp_TZ: convert to UTC -> 'YYYY-MM-DD HH24:MI:SS'
#   - Date       : 'YYYY-MM-DD'
#   - Text/Char  : TRIM leading/trailing spaces
#   - UUID       : UPPER(TRIM()) — case-insensitive comparison
#   - Integer    : CAST to text
#   - JSON/JSONB : canonical serialization (jsonb::text / TO_JSON)
#   - Bytea      : hex text encoding
#   - NULL       : COALESCE -> '<<NULL>>' sentinel (ALL columns)
# ============================================================

tables:
  customers:
    validations:

      # ── ③ / ④ Normalised data validation (all columns) ───────────
      data_validation:
        source_table_name: customers
        source: postgresql
        source_database: my_test_pg
        source_schema: public
        pksourcecolumn: id_normalized
        source_audit_column: 
        sourcequery: |
          SELECT COALESCE(CAST(CAST(id AS TEXT) AS TEXT), '<<NULL>>') AS id_normalized, COALESCE(CAST(TRIM(first_name) AS TEXT), '<<NULL>>') AS first_name_normalized, COALESCE(CAST(TRIM(last_name) AS TEXT), '<<NULL>>') AS last_name_normalized, COALESCE(CAST(TRIM(email) AS TEXT), '<<NULL>>') AS email_normalized, COALESCE(CAST(TRIM(phone) AS TEXT), '<<NULL>>') AS phone_normalized, COALESCE(CAST(CASE WHEN is_active = true THEN '1' WHEN is_active = false THEN '0' ELSE NULL END AS TEXT), '<<NULL>>') AS is_active_normalized, COALESCE(CAST(CAST(CAST(balance AS NUMERIC) AS TEXT) AS TEXT), '<<NULL>>') AS balance_normalized, COALESCE(CAST(TO_CHAR(signup_date, 'YYYY-MM-DD') AS TEXT), '<<NULL>>') AS signup_date_normalized, COALESCE(CAST(TO_CHAR(created_at, 'YYYY-MM-DD HH24:MI:SS.US') AS TEXT), '<<NULL>>') AS created_at_normalized, COALESCE(CAST(TO_CHAR(updated_at, 'YYYY-MM-DD HH24:MI:SS.US') AS TEXT), '<<NULL>>') AS updated_at_normalized FROM public.customers;
        target_table_name: CUSTOMERS
        target: snowflake
        target_database: MV_TEST
        target_schema: PUBLIC
        pktargetcolumn: id_normalized
        target_audit_column: 
        targetquery: |
          SELECT COALESCE(CAST(CAST(ID AS STRING) AS STRING), '<<NULL>>') AS "id_normalized", COALESCE(CAST(TRIM(FIRST_NAME) AS STRING), '<<NULL>>') AS "first_name_normalized", COALESCE(CAST(TRIM(LAST_NAME) AS STRING), '<<NULL>>') AS "last_name_normalized", COALESCE(CAST(TRIM(EMAIL) AS STRING), '<<NULL>>') AS "email_normalized", COALESCE(CAST(TRIM(PHONE) AS STRING), '<<NULL>>') AS "phone_normalized", COALESCE(CAST(CASE WHEN IS_ACTIVE = TRUE THEN '1' WHEN IS_ACTIVE = FALSE THEN '0' ELSE NULL END AS STRING), '<<NULL>>') AS "is_active_normalized", COALESCE(CAST(CAST(BALANCE AS STRING) AS STRING), '<<NULL>>') AS "balance_normalized", COALESCE(CAST(TO_VARCHAR(SIGNUP_DATE, 'YYYY-MM-DD') AS STRING), '<<NULL>>') AS "signup_date_normalized", COALESCE(CAST(TO_VARCHAR(CREATED_AT, 'YYYY-MM-DD HH24:MI:SS.FF6') AS STRING), '<<NULL>>') AS "created_at_normalized", COALESCE(CAST(TO_VARCHAR(UPDATED_AT, 'YYYY-MM-DD HH24:MI:SS.FF6') AS STRING), '<<NULL>>') AS "updated_at_normalized" FROM MV_TEST.PUBLIC.CUSTOMERS;

      validation_plan:
        population_scope: {}
        relationships: []
        identity:
          type: primary_key
          source_primary_keys:
          - id
          target_primary_keys:
          - ID
          candidate_keys: []
        row_hash: null
        transformations: []
        validations: []
        requires_review: false
        review_reasons: []

      transformation_validation:
        source_table_name: customers
        source: postgresql
        source_database: my_test_pg
        source_schema: public
        sourcequery: |
          SELECT 1;
        target_table_name: CUSTOMERS
        target: snowflake
        target_database: MV_TEST
        target_schema: PUBLIC
        targetquery: |
          SELECT 1;
      aggregate_validation:
        source_table_name: customers
        source: postgresql
        source_database: my_test_pg
        source_schema: public
        sourcequery: |
          SELECT 1;
        target_table_name: CUSTOMERS
        target: snowflake
        target_database: MV_TEST
        target_schema: PUBLIC
        targetquery: |
          SELECT 1;