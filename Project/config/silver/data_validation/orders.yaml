# ============================================================
# Migration Validator — Data Validation Config
# Table      : orders
# Generated  : 2026-09-13T14:28:50.079658
# By         : ai (model: gpt-4o)
# Columns    : 9 comparable columns
#
# Validation blocks (row count is in bronze.yaml):
#   data_validation           — normalised full-scan SELECT (all columns)
#
# Note: null_pct_validation and distinct_count_validation have been
# removed. Only data_validation and count_validation are used.
#
# Normalization rules applied automatically:
#   - Boolean    : TRUE/FALSE -> '1'/'0'
#   - Numeric    : ROUND to 2 decimal places, then text
#   - Timestamp  : 'YYYY-MM-DD HH24:MI:SS'  (microseconds stripped)
#   - Timestamp_TZ: convert to UTC -> 'YYYY-MM-DD HH24:MI:SS'
#   - Date       : 'YYYY-MM-DD'
#   - Text/Char  : TRIM leading/trailing spaces
#   - UUID       : UPPER(TRIM()) — case-insensitive comparison
#   - Integer    : CAST to text
#   - JSON/JSONB : canonical serialization (jsonb::text / TO_JSON)
#   - Bytea      : hex text encoding
#   - NULL       : COALESCE -> '<<NULL>>' sentinel (ALL columns)
# ============================================================

tables:
  orders:
    validations:

      # ── ③ / ④ Normalised data validation (all columns) ───────────
      data_validation:
        source_table_name: orders
        source: postgresql
        source_database: my_test_pg
        source_schema: public
        pksourcecolumn: id_normalized
        source_audit_column: 
        sourcequery: |
          SELECT COALESCE(CAST(CAST(id AS TEXT) AS TEXT), '<<NULL>>') AS id_normalized, COALESCE(CAST(CAST(customer_id AS TEXT) AS TEXT), '<<NULL>>') AS customer_id_normalized, COALESCE(CAST(TO_CHAR(order_date, 'YYYY-MM-DD') AS TEXT), '<<NULL>>') AS order_date_normalized, COALESCE(CAST(TRIM(status) AS TEXT), '<<NULL>>') AS status_normalized, COALESCE(CAST(CAST(CAST(total_amount AS NUMERIC) AS TEXT) AS TEXT), '<<NULL>>') AS total_amount_normalized, COALESCE(CAST(TRIM(notes) AS TEXT), '<<NULL>>') AS notes_normalized, COALESCE(CAST(CASE WHEN is_paid = true THEN '1' WHEN is_paid = false THEN '0' ELSE NULL END AS TEXT), '<<NULL>>') AS is_paid_normalized, COALESCE(CAST(TO_CHAR(created_at, 'YYYY-MM-DD HH24:MI:SS.US') AS TEXT), '<<NULL>>') AS created_at_normalized, COALESCE(CAST(TO_CHAR(updated_at, 'YYYY-MM-DD HH24:MI:SS.US') AS TEXT), '<<NULL>>') AS updated_at_normalized FROM public.orders;
        target_table_name: ORDERS
        target: snowflake
        target_database: MV_TEST
        target_schema: PUBLIC
        pktargetcolumn: id_normalized
        target_audit_column: 
        targetquery: |
          SELECT COALESCE(CAST(CAST(ID AS STRING) AS STRING), '<<NULL>>') AS "id_normalized", COALESCE(CAST(CAST(CUSTOMER_ID AS STRING) AS STRING), '<<NULL>>') AS "customer_id_normalized", COALESCE(CAST(TO_VARCHAR(ORDER_DATE, 'YYYY-MM-DD') AS STRING), '<<NULL>>') AS "order_date_normalized", COALESCE(CAST(TRIM(STATUS) AS STRING), '<<NULL>>') AS "status_normalized", COALESCE(CAST(CAST(TOTAL_AMOUNT AS STRING) AS STRING), '<<NULL>>') AS "total_amount_normalized", COALESCE(CAST(TRIM(NOTES) AS STRING), '<<NULL>>') AS "notes_normalized", COALESCE(CAST(CASE WHEN IS_PAID = TRUE THEN '1' WHEN IS_PAID = FALSE THEN '0' ELSE NULL END AS STRING), '<<NULL>>') AS "is_paid_normalized", COALESCE(CAST(TO_VARCHAR(CREATED_AT, 'YYYY-MM-DD HH24:MI:SS.FF6') AS STRING), '<<NULL>>') AS "created_at_normalized", COALESCE(CAST(TO_VARCHAR(UPDATED_AT, 'YYYY-MM-DD HH24:MI:SS.FF6') AS STRING), '<<NULL>>') AS "updated_at_normalized" FROM MV_TEST.PUBLIC.ORDERS;

      validation_plan:
        population_scope: {}
        relationships: []
        identity:
          type: primary_key
          source_primary_keys:
          - id
          target_primary_keys:
          - ID
          candidate_keys: []
        row_hash: null
        transformations: []
        validations: []
        requires_review: false
        review_reasons: []

      transformation_validation:
        source_table_name: orders
        source: postgresql
        source_database: my_test_pg
        source_schema: public
        sourcequery: |
          SELECT 1;
        target_table_name: ORDERS
        target: snowflake
        target_database: MV_TEST
        target_schema: PUBLIC
        targetquery: |
          SELECT 1;
      aggregate_validation:
        source_table_name: orders
        source: postgresql
        source_database: my_test_pg
        source_schema: public
        sourcequery: |
          SELECT 1;
        target_table_name: ORDERS
        target: snowflake
        target_database: MV_TEST
        target_schema: PUBLIC
        targetquery: |
          SELECT 1;