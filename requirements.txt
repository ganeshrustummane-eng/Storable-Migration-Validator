# Migration Validator PoC - Python Dependencies

# AI agent dependencies
openai          # DIAL uses the OpenAI-compatible API
anthropic       # Anthropic-compatible API
python-dotenv    # Load DIAL_API_KEY from .env and .env credentials

# Fuzzy matching — used by the new matching pipeline
rapidfuzz       # Fast Levenshtein/token_ratio; graceful fallback to difflib if absent

# Database Drivers
# Lower bounds chosen so prebuilt wheels exist for Python 3.13/3.14 (older pins force source builds)
psycopg2-binary       # PostgreSQL
pyodbc                # Microsoft SQL Server
snowflake-connector-python  # Snowflake
boto3                 # AWS Athena
pyathena              # AWS Athena column extraction (extractors.py:AthenaExtractor) — imported but was missing here

# Data Processing
pandas          # DataFrame handling for validation
pyyaml             # YAML config file parsing
pydantic           # Schema validation for generated YAML configs (fail at load, not mid-run)

# Optional: For better JSON handling
python-dateutil

# Optional: For enhanced CLI support
click

# Web UI (webapp/app.py) — optional, only needed to run the browser-based UI
streamlit

# Gemini Migration Intelligence Connector
# Install for Gemini Enterprise integration:
google-genai>=0.8.0          # Gemini function-calling agent (replaces deprecated google-generativeai)
fastapi>=0.110.0              # REST API server for Gemini connector
uvicorn[standard]             # ASGI server for FastAPI
PyJWT>=2.8.0                  # JWT token validation (auth layer)
httpx>=0.27.0                 # async HTTP client (FastAPI TestClient)
requests>=2.31.0               # Jira REST API client (Review & Approve ticket creation)
apscheduler>=3.10.0            # Scheduled validation runs (sidebar scheduler UI)

# Development dependencies (optional)
# For testing
pytest
pytest-cov

# For code quality
flake8
black
pylint
